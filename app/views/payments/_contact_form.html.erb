<!--       <%= simple_form_for [@contact] do |f| %>
      <div style="display: grid; grid-template-columns: 1fr 1fr; grid-column-gap: 16px">
        <%= f.input :first_name, input_html: { class: 'search-fields' }  %>
        <%= f.input :last_name, input_html: { class: 'search-fields' } %>
        <%= f.input :email, input_html: { class: 'search-fields' } %>
        <%= f.input :phone, input_html: { class: 'search-fields' } %>
        <%= f.input :saved, label: "Save contact info for future orders" %>
      </div>
      <div class="mb-5">
      <%= f.submit "Add item", class: "btn search-button-styled mt-2 form-control", style: "width: 100%;" %>
      </div>
    <% end %>
 -->

    <form id="contact-address-form" action="<%= addContact_order_path(params[:order_id]) %>" method="POST">
      <input name="authenticity_token" value="<%= form_authenticity_token %>" type="hidden">
      <div style="display: grid; grid-template-columns: 1fr 1fr; grid-column-gap: 16px">
        <div class="form-group">
        <label for="first_name">First Name</label>
        <input class="form-control" type="text" id="first_name" name="first_name" <% if current_user.contacts.length > 0 %>value="<%= current_user.contacts[0].first_name  %> " <% end %>>
      </div>
      <div class="form-group">
        <label for="last_name">Last Name</label>
        <input class="form-control" type="text" id="last_name" name="last_name" <% if current_user.contacts.length > 0 %>value="<%= current_user.contacts[0].last_name  %> " <% end %>>
      </div>
      <div class="form-group">
        <label for="email">Email</label>
        <input class="form-control" type="text" id="email" name="email" <% if current_user.contacts.length > 0 %>value="<%= current_user.contacts[0].email  %> " <% end %>>
      </div>
      <div class="form-group">
        <label for="phone">Phone Number</label>
        <input class="form-control" type="text" id="phone" name="phone" <% if current_user.contacts.length > 0 %>value="<%= current_user.contacts[0].phone  %> " <% end %>>
      </div>
      <% if current_user.contacts.length == 0 %>
        <div class="form-check">
          <input id="save_contact" class="form-check-input" name="saved" type="checkbox" value="true">
          <label class="form-check-label" for="save_contact">Save contact info for future orders</label>
        </div>
      <% else %>
        <a href="">Edit your contact info</a>
      <% end %>
      </div>
      <input type="submit">
    </form>



